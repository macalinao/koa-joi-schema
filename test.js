const expect = require('chai').expect
const validate = require('.')

describe('koa-joi-schema', () => {

  it('should check schema on body', () => {
    const validateBody = validate('body')
  })

})
